interface:
  display_name: "vidIQ Retention Analysis"
  short_description: "Map retention drops to video evidence"
  default_prompt: "Use $retention-analysis to map where viewers leave and assess competing causes."

dependencies:
  tools:
    - type: "mcp"
      value: "vidiq"
      description: "Live creator intelligence from vidIQ"
      transport: "streamable_http"
      url: "https://mcp.vidiq.com/mcp"
